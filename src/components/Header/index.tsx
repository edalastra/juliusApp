import React from "react";
import { Header, Title } from "../GlobalComponents/styles";

const HeaderComponent : React.FC = () => {
    return (
        <Header>
            <Title>JULIUS APP</Title>
        </Header>
    );
}

export default HeaderComponent;